# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
pyautogui>=0.9.54
Pillow>=10.0.0

# Audio feedback
pygame>=2.5.0

# SocketIO client
python-socketio>=5.10.0
websocket-client>=1.6.0
flask-socketio>=5.5.0

# OCR
easyocr>=1.7.0

# Windows automation
pywin32>=306
comtypes>=1.4.0

# UI Automation for enhanced desktop app readiness detection (optional but recommended)
# Provides deterministic state detection for desktop applications
# Graceful fallback to basic window checks if not installed
uiautomation>=2.0.0  # Optional: Enhanced desktop app detection

# File watching (for run_client.py)
watchdog>=3.0.0

# Process management
psutil>=5.9.0

# Gemini AI for Vision Service
google-genai

# Note: GPU acceleration not available for older GPUs (compute capability < 3.7)

# Settings Interface dependencies
pywebview>=5.0.0
hypothesis>=6.0.0  # For property-based testing

# Firebase integration
firebase-admin>=6.0.0

# QR code generation
qrcode[pil]>=7.4.0

# System tray icon
pystray>=0.19.0

# Packaging tool for creating distributable executables
pyinstaller>=6.0.0

# AI Editor Dependencies
openai>=1.40.0
pydantic>=2.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
requests>=2.31.0
